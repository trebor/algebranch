# Dependency directories
node_modules/
/ui/node_modules/
/math-engine/node_modules/

# Production build outputs
/ui/.next/
/ui/out/
/math-engine/dist/

# Test coverage reports
/math-engine/coverage/

# Environment / secrets — never commit local env files
.env
.env.*
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# System files
.DS_Store
Thumbs.db

# Screenshots and temporary layout captures
/*.png
# Unanchored so it also covers workspace-local capture dirs (e.g. ui/screenshots/
# from `npm run shoot` run inside the ui workspace), not just the repo root.
screenshots/
